{{!-- Comment Modals --}}
{{#each articles}}
  {{>articles/comments}}
{{/each}}

{{!-- Nav --}}
<div class="page-title">
  <span>New York Times </span>
  <span class="smaller">newsScraper App</span>
</div>

{{!-- Header Image --}}
<img class="responsive-img z-depth-1" src="newsstand-img.png">

{{!-- Container for Article Cards --}}
<div class="row" style="margin-top: 50px; margin-left: 50px; margin-right: 50px">
  {{#each articles}}
    {{>articles/article-card}}
  {{/each}}
</div>

